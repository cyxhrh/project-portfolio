> 原项目历史 QA 记录，记录日期 2026-07-22；不是本次重跑测试的结果。

# D-011-QA 标本馆月度章节回看

日期：2026-07-22  
构建：`glimmer-web-0.1.0-d011.20260722`（本地 production preview，未部署）

## 结论

通过发布前验收，当前范围无开放 P0／P1／P2 缺陷。建议进入项目负责人的发布决策流程。

## 静态门禁

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 通过：6 个测试文件、25 项测试 |
| `npm run build` | 通过 |

## 生产预览浏览器验收

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| 空馆 | 通过 | `#gallery` 显示总数 0、无月份章节，仅有“去制作一张”。 |
| 1 张 | 通过 | 真实创建并保存 1 张“雨后”卡；显示 2026 年 7 月及“这个月，你留下了 1 份微光”。 |
| 2 张与心情并列 | 通过 | 再保存 1 张“晨雾”卡；总数更新为 2，月份总结回退为数量型文案，未错误指定单一最高频心情。 |
| 月份连续、跨月排序 | 静态通过 | `galleryRevisit.test.ts` 覆盖跨月倒序与本地月边界；本次浏览器会话未能注入跨月 `createdAt` 数据。 |
| 无类型旧卡 | 静态通过 | 单元测试覆盖无 `type` 的旧卡；列表元信息只在 `type` 存在时追加类型。 |
| 320px | 通过 | 320 × 800：两列各 134px、列距 12px、文档宽 320px，无横向溢出。 |
| 桌面 | 通过 | 1280px：卡片为 240px 的自适应网格，无横向溢出。 |
| 独立详情和返回 | 通过 | 进入详情时焦点落在 `#gallery-detail-title`；返回后焦点恢复至原缩略卡。 |
| 确认删除与更新 | 通过 | 确认删除一张后总数由 2 更新为 1，月份总结随即改为单卡文案。 |
| 取消删除 | 未覆盖 | 自动化会话的原生 `confirm` 对话框无法稳定执行“取消”分支；未发现实现层面的异常。 |
| 下载 | 未覆盖 | 详情页“保存图片”已在浏览器中确认可用；未执行系统级保存／落盘。 |
| 背景 404 占位 | 未覆盖 | 当前会话不支持请求拦截，无法注入单张背景 404；既有故障注入回归不在本轮重新执行。 |

## 缺陷

无开放 P0／P1／P2。本轮未发现可复现缺陷。

## 未覆盖风险

1. 真机 iPhone Safari、Android Chrome、macOS Safari 的 320px 布局、系统保存和原生删除确认仍需人工冒烟。
2. 跨月真实 IndexedDB 数据、无类型遗留记录及单张背景 404 未在本轮生产预览浏览器中注入；相应纯逻辑测试通过。
3. 大规模馆藏会生成完整 Canvas 缩略预览，滚动和内存表现仍需以真实数据量复测。
